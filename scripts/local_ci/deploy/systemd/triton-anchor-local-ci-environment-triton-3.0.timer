# EXAMPLE ONLY: regenerate as the actual CI user; never install placeholders.
[Unit]
Description=Staggered trusted Local CI image refresh

[Timer]
OnCalendar=*-*-* 02:00:00 Asia/Shanghai
Persistent=true
RandomizedDelaySec=60

[Install]
WantedBy=timers.target
