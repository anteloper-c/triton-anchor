# Copy outside the Git checkout; owner jiwang_ci, mode 600.
# No export prefix. Use shell/systemd-compatible quoting when filling values.
# Do not commit a filled file or paste secrets into the deployment conversation.
GITEE_USERNAME=
GITEE_TOKEN=
GITEE_HEALTH_TOKEN=
LOCAL_CI_SMTP_HOST=
LOCAL_CI_SMTP_PORT=
LOCAL_CI_SMTP_USERNAME=
LOCAL_CI_SMTP_PASSWORD=
LOCAL_CI_SMTP_FROM=
LOCAL_CI_SMTP_TO=
LOCAL_CI_SMTP_STARTTLS=
# Add only the actual company provider env_key/env_http_headers variables
# required by the existing dedicated Codex config. Do not invent a model/key.
