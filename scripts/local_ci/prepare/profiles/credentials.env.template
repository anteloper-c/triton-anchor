# Copy outside the Git checkout; owned by the CI user, mode 600.
# No export prefix. Use shell/systemd-compatible quoting when filling values.
# Keep the filled file private and outside Git.
GITEE_USERNAME=
GITEE_TOKEN=
GITEE_HEALTH_TOKEN=
# Add only the actual model provider env_key/env_http_headers variables
# required by the existing dedicated Codex config. Do not invent a model/key.
