# 任务内辅助工具

Codex 可按 PR 需要在任务目录中编写计划、定向测试、复现、基线比较和日志整理脚本，
直接通过 shell 执行。优先复用已有测试；辅助脚本只服务于当前任务。

构建、测试与审查可以交错进行。保留简短计划，说明已完成、待验证和证据位置，
便于中断后继续。环境修复或修改源码后的结果应说明改动与适用范围，
不能把尚未运行、全部跳过或修改断言后的测试写成原始提交已通过。

最终选择必要脚本、失败日志、IR、测量结果等重要文件，连同摘要写入 Gitee 结果仓库；
缓存、完整构建目录和无关调试记录留在本地。永久代码或环境改进由维护者另行采纳。
